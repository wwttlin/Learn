# 📤 專案上傳方案（無需 gcloud CLI）

## 🎯 方案選擇

### 方案1：使用 SCP/SFTP（推薦）
### 方案2：使用 Git 倉庫
### 方案3：使用 FTP 工具
### 方案4：使用 Web 界面上傳
### 方案5：使用 rsync

---

## 🔧 方案1：SCP/SFTP 上傳（推薦）

### Windows 用戶

#### 使用 WinSCP（圖形界面）
1. **下載安裝 WinSCP**：https://winscp.net/
2. **連接設定**：
   - 檔案協定：SFTP
   - 主機名稱：你的 GCP VM 外部 IP
   - 端口：22
   - 用戶名：你的 VM 用戶名
   - 密碼：你的 VM 密碼（或使用 SSH 金鑰）

3. **上傳步驟**：
   ```
   1. 連接到 VM
   2. 在右側（遠端）建立目錄：/home/username/
   3. 將整個 tutoring-system 資料夾拖拽到右側
   4. 等待上傳完成
   ```

#### 使用 PowerShell SCP
```powershell
# 安裝 OpenSSH（Windows 10/11 通常已內建）
# 上傳整個專案資料夾
scp -r .\tutoring-system\ username@your-vm-ip:~/

# 或者先壓縮再上傳（更快）
Compress-Archive -Path .\tutoring-system\ -DestinationPath tutoring-system.zip
scp tutoring-system.zip username@your-vm-ip:~/
```

### Linux/Mac 用戶

#### 使用 SCP 命令
```bash
# 直接上傳資料夾
scp -r ./tutoring-system/ username@your-vm-ip:~/

# 或先壓縮再上傳
tar -czf tutoring-system.tar.gz tutoring-system/
scp tutoring-system.tar.gz username@your-vm-ip:~/
```

#### 使用 rsync（更高效）
```bash
# 同步上傳（支援斷點續傳）
rsync -avz -e ssh ./tutoring-system/ username@your-vm-ip:~/tutoring-system/

# 排除不必要的檔案
rsync -avz -e ssh --exclude 'node_modules' --exclude '.git' ./tutoring-system/ username@your-vm-ip:~/tutoring-system/
```

---

## 🐙 方案2：使用 Git 倉庫（最簡單）

### 步驟1：建立 Git 倉庫
```bash
# 在本地專案目錄
git init
git add .
git commit -m "Initial commit"

# 推送到 GitHub/GitLab
git remote add origin https://github.com/your-username/tutoring-system.git
git push -u origin main
```

### 步驟2：在 VM 上下載
```bash
# SSH 連接到 VM
ssh username@your-vm-ip

# 下載專案
git clone https://github.com/your-username/tutoring-system.git
cd tutoring-system

# 執行部署
chmod +x deploy.sh
./deploy.sh
```

### 優點
- ✅ 最簡單的方式
- ✅ 支援版本控制
- ✅ 可以隨時更新
- ✅ 不需要額外工具

---

## 📁 方案3：FTP 工具上傳

### 使用 FileZilla
1. **下載 FileZilla**：https://filezilla-project.org/
2. **連接設定**：
   - 協定：SFTP
   - 主機：your-vm-ip
   - 端口：22
   - 用戶：your-username
   - 密碼：your-password

3. **上傳操作**：
   - 左側選擇本地專案資料夾
   - 右側導航到 /home/username/
   - 拖拽上傳整個資料夾

---

## 🌐 方案4：Web 界面上傳

### 在 VM 上設定簡單的上傳服務
```bash
# SSH 連接到 VM
ssh username@your-vm-ip

# 安裝 Python（通常已安裝）
python3 --version

# 建立上傳目錄
mkdir -p ~/uploads
cd ~/uploads

# 啟動簡單的 HTTP 服務器（支援上傳）
python3 -m http.server 8080

# 或使用 Node.js 上傳服務
npm install -g http-server
http-server -p 8080
```

然後在瀏覽器訪問：`http://your-vm-ip:8080`

---

## 📦 方案5：壓縮包上傳

### 步驟1：本地壓縮
```bash
# Windows (PowerShell)
Compress-Archive -Path .\tutoring-system\ -DestinationPath tutoring-system.zip

# Linux/Mac
tar -czf tutoring-system.tar.gz tutoring-system/
# 或
zip -r tutoring-system.zip tutoring-system/
```

### 步驟2：上傳壓縮包
```bash
# 使用 SCP
scp tutoring-system.zip username@your-vm-ip:~/

# 或使用任何 FTP 工具上傳 zip 檔案
```

### 步驟3：在 VM 上解壓
```bash
# SSH 連接到 VM
ssh username@your-vm-ip

# 解壓檔案
unzip tutoring-system.zip
# 或
tar -xzf tutoring-system.tar.gz

# 進入目錄並部署
cd tutoring-system
chmod +x deploy.sh
./deploy.sh
```

---

## 🔑 SSH 連接設定

### 獲取 VM 連接資訊
1. **登入 GCP Console**：https://console.cloud.google.com/
2. **進入 Compute Engine > VM 執行個體**
3. **找到你的 VM**，記錄：
   - 外部 IP 位址
   - 用戶名（通常是你的 Google 帳號名稱）

### 設定 SSH 金鑰（推薦）
```bash
# 生成 SSH 金鑰對
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"

# 複製公鑰內容
cat ~/.ssh/id_rsa.pub

# 在 GCP Console 中：
# Compute Engine > 中繼資料 > SSH 金鑰 > 編輯 > 新增項目
# 貼上公鑰內容
```

### SSH 連接
```bash
# 使用密碼連接
ssh username@your-vm-ip

# 使用金鑰連接
ssh -i ~/.ssh/id_rsa username@your-vm-ip
```

---

## 🚀 完整部署流程範例

### 使用 Git 方案（推薦）

#### 1. 本地準備
```bash
# 建立 .gitignore（排除不必要檔案）
echo "node_modules/" > .gitignore
echo "*.db" >> .gitignore
echo "logs/" >> .gitignore

# 初始化 Git
git init
git add .
git commit -m "補習班管理系統初始版本"

# 推送到 GitHub（需要先在 GitHub 建立倉庫）
git remote add origin https://github.com/your-username/tutoring-system.git
git push -u origin main
```

#### 2. VM 部署
```bash
# SSH 連接
ssh username@your-vm-ip

# 下載專案
git clone https://github.com/your-username/tutoring-system.git
cd tutoring-system

# 一鍵部署
chmod +x deploy.sh
./deploy.sh
```

#### 3. 訪問系統
- 前端：http://your-vm-ip:3000
- 後端：http://your-vm-ip:5000

---

## 💡 小技巧

### 1. 加速上傳
```bash
# 排除不必要的檔案
rsync -avz --exclude 'node_modules' --exclude '.git' --exclude '*.db' ./tutoring-system/ username@your-vm-ip:~/tutoring-system/
```

### 2. 斷點續傳
```bash
# 使用 rsync 支援斷點續傳
rsync -avz --partial --progress ./tutoring-system/ username@your-vm-ip:~/tutoring-system/
```

### 3. 壓縮上傳
```bash
# 先壓縮再上傳（節省時間）
tar -czf - tutoring-system/ | ssh username@your-vm-ip "cd ~ && tar -xzf -"
```

選擇最適合你的方案開始部署吧！🎉